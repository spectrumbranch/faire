var assert = require('assert');

console.log(process.env.NODE_ENV);

describe('Faire Library API', function() {
	describe('#function()', function() {
		it('should do stuff.', function(done) {
			done()
		})
	})
})